# What this changes

<!-- What does this do, and why? The diff shows what changed; explain the reason. -->

Closes #

## How I tested it

<!-- What you actually ran or clicked. "Tests pass" alone isn't enough for UI changes. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck` and `npm test` all pass
- [ ] Added or updated tests for any new logic
- [ ] Checked the change in **both light and dark** themes (if it touches the UI)
- [ ] No new network requests, no shell interpolation, no weakening of the CSP
      or sandbox (see [ARCHITECTURE.md](../ARCHITECTURE.md))
- [ ] Updated `CHANGELOG.md` under `[Unreleased]` if this is user-visible
